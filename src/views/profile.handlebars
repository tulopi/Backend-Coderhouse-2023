<div class="profile">
    <h2>Profile</h2>
    <div class="profile-details">
        <p><strong>Welcome:</strong> {{user.first_name}}</p>
        <p><strong>Email:</strong> {{user.email}}</p>
    </div>
    <div>
        <button class="btn btn-primary" onclick="window.location.href = 'api/sessions/signout'">Sign out</button>
    </div>
</div>